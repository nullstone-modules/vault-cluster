# Static validation.
name: validate

on:
  push:
    branches: [main, master, develop]
  pull_request:

permissions:
  contents: read

jobs:
  go:
    name: go unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.23.x"

      - name: Unit tests (no Docker)
        run: go test -short ./...

  shellcheck:
    name: shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck

      - name: Lint all shell scripts
        run: |
          set -euo pipefail
          mapfile -t files < <(find config scripts tests local -name '*.sh' | sort)
          printf 'checking %d scripts\n' "${#files[@]}"
          # -x follows sourced files, so cross-file problems are caught too.
          shellcheck -x --severity=warning --shell=bash "${files[@]}"

      - name: Every script must be executable
        run: |
          set -euo pipefail
          # A non-executable script fails at the least convenient moment, in
          # someone else's environment, with a confusing error.
          fail=0
          while IFS= read -r f; do
            case "$f" in */lib/*|*/lib.sh|*/common/*) continue ;; esac
            [ -x "$f" ] || { echo "not executable: $f"; fail=1; }
          done < <(find config scripts tests local -name '*.sh' | sort)
          exit "$fail"

  architecture:
    name: runtime separation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: shared config/scripts/tests must not reference any runtime
        # Shared config must not depend on a runtime. Enforced mechanically.
        # A rule checked only at review time erodes; this fails the build.
        run: |
          set -euo pipefail
          fail=0
          check() {
            local pattern="$1" label="$2"
            if grep -rInE "$pattern" config/ scripts/ tests/ --include='*.sh' --include='*.tpl' --include='*.hcl' \
                 | grep -vE '^\s*[^:]+:[0-9]+:\s*#' ; then
              echo "FAIL: shared tree references $label"
              fail=1
            fi
          }
          # Comment lines are excluded above: the prohibition is on depending
          # on a runtime, not on explaining why the rule exists.
          check '\bdocker\b'              'docker'
          check '\bdocker[- ]compose\b'   'docker compose'
          check 'vault-cluster-(vault|postgres)'  'container names'
          check '/vault/(file|logs|config)' 'container filesystem paths'
          exit "$fail"

      - name: Conformance tests must be driven only by VAULT_ADDR
        run: |
          set -euo pipefail
          if grep -rIn 'docker' tests/conformance/ | grep -vE ':\s*#'; then
            echo "FAIL: conformance tests reference docker."
            echo "Runtime-specific assertions belong in local/tests/."
            exit 1
          fi

  security:
    name: secret scan and CE guard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: No bootstrap material is committed
        run: |
          set -euo pipefail
          fail=0
          # Names first: these files must never exist in the tree at all.
          while IFS= read -r f; do
            echo "FAIL: bootstrap material committed: $f"; fail=1
          done < <(find . -path ./.git -prune -o \
                     \( -name 'vault-init.json' -o -name '*.snap' \
                        -o -name '*.token' -o -name 'unseal*' \) -print)

          # Then content: an unseal key or root token pasted into a doc.
          if grep -rInE 'hvs\.[A-Za-z0-9_-]{20,}|"root_token"\s*:\s*"hvs' . \
               --exclude-dir=.git --exclude=CHANGELOG.md; then
            echo "FAIL: what looks like a real Vault token is committed"; fail=1
          fi
          exit "$fail"

      - name: Fake credentials are labelled as fake
        # Fixture values that look plausible eventually get copied somewhere
        # real. Everything in this repo is prefixed FAKE- or local-dev-.
        run: |
          set -euo pipefail
          if grep -rInE '(password|secret|api_key)\s*[:=]\s*"[A-Za-z0-9]{16,}"' \
               config/ scripts/ tests/ 2>/dev/null \
               | grep -viE 'FAKE-|local-dev-|\{\{|\$\{|not-a-real'; then
            echo "FAIL: an unlabelled credential-shaped literal was found"
            exit 1
          fi

      - name: No Vault Enterprise features are used
        # Community Edition only. An Enterprise-only endpoint
        # works against a trial binary and fails on the licensed-out one, which
        # is the worst possible time to find out.
        run: |
          set -euo pipefail
          fail=0
          # sys/namespaces, control groups, performance replication, Sentinel.
          if grep -rInE 'sys/namespaces|X-Vault-Namespace|sys/replication|sys/control-group|sentinel' \
               config/ scripts/ tests/ local/ cmd/ internal/ --include='*.sh' --include='*.hcl' --include='*.tpl' --include='*.go' \
               | grep -vE ':\s*#' | grep -v 'VAULT_NAMESPACE'; then
            echo "FAIL: Vault Enterprise-only feature referenced (Community Edition only)"
            fail=1
          fi
          if grep -rIn 'hashicorp/vault-enterprise' config scripts tests local --exclude-dir=.git; then
            echo "FAIL: Enterprise image referenced"; fail=1
          fi
          exit "$fail"

      - name: Images are pinned by digest
        run: |
          set -euo pipefail
          fail=0
          if grep -nE '^[A-Z_]*IMAGE=' local/.env.example | grep -v '@sha256:'; then
            echo "FAIL: an image is not digest-pinned"; fail=1
          fi
          if grep -rn ':latest' local/ --include='*.yml' --include='.env.example'; then
            echo "FAIL: ':latest' found - the environment could change without a commit"; fail=1
          fi
          exit "$fail"

  compose:
    name: compose config
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compose file is valid and PostgreSQL is profile-gated
        run: |
          set -euo pipefail
          cd local
          cp .env.example .env

          docker compose --env-file .env config --quiet

          default_svcs="$(docker compose --env-file .env config --services)"
          printf 'default services:\n%s\n' "${default_svcs}"
          if printf '%s\n' "${default_svcs}" | grep -qx 'unseal'; then
            echo "FAIL: long-running unseal sidecar should not be a default service"
            exit 1
          fi
          if ! printf '%s\n' "${default_svcs}" | grep -qx 'bootstrap'; then
            echo "FAIL: bootstrap one-shot missing from default compose services"
            exit 1
          fi
          if printf '%s\n' "${default_svcs}" | grep -qx 'postgres'; then
            echo "FAIL: postgres appears without the credentials profile"
            exit 1
          fi

          cred_svcs="$(COMPOSE_PROFILES=credentials docker compose --env-file .env --profile credentials config --services)"
          printf 'credentials services:\n%s\n' "${cred_svcs}"
          if ! printf '%s\n' "${cred_svcs}" | grep -qx 'postgres'; then
            echo "FAIL: postgres missing from the credentials profile"
            exit 1
          fi

          grep -A20 '^  postgres:' docker-compose.yml | grep -q 'credentials' \
            || { echo "FAIL: postgres is not declared behind the credentials profile in docker-compose.yml"; exit 1; }

  docs:
    name: documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Required documents exist
        run: |
          set -euo pipefail
          [ -f README.md ] || { echo "missing: README.md"; exit 1; }
          [ -f CHANGELOG.md ] || { echo "missing: CHANGELOG.md"; exit 1; }
